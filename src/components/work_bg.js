import React, { useEffect, useRef } from "react";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

import Table from '../images/table-laptop-light.svg'

const HomeBg = ({  }) => {
  
  useEffect(() => {
  
  },[])

  return (
    <div className="bg_anim work">
      <Table />
    </div>
  );
};
 
export default HomeBg;